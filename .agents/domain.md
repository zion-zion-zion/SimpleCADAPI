# Domain Docs

This repository now has a single product: the CadFlow skill under
`skills/cadflow/`. There is no Harness application glossary.

When the work is CAD modeling, read `skills/cadflow/SKILL.md` and the
reference it points at. When the work is packaging or tests, read
`README.md` and `AGENTS.md`.
